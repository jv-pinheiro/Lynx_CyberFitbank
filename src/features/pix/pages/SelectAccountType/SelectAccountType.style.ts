import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  accountTypeHeader: {
    marginTop: 24,
    marginBottom: 16,
    "& .MuiTypography-root": {
      fontWeight: 500,
    },
  },
  selectionCards: {
    margin: "0 -16px",
  },
});
