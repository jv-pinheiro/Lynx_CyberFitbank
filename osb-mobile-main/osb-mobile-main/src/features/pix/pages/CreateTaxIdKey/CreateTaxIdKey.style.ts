import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles({
  taxIdInput: {
    marginTop: '10px',
  },
  importantWarning: {
    marginTop: '30px',
    fontWeight: 500,
  },
  textAlert: {
    fontSize: 15,
    marginTop: 10,
  },
})
