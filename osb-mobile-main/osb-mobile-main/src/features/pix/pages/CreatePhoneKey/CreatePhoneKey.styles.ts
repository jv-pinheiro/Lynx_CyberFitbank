import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  phoneInput: {
    marginTop: "10px",
  },
  importantWarning: {
    marginTop: "30px",
  },

  txtalert: {
    fontSize: 15,
    marginTop: 10,
  },
});
