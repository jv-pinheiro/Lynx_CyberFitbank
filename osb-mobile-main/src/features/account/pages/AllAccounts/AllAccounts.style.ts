import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles({
  currentAccount: {
    marginTop: 16,
    backgroundColor: 'transparent',
  },
  headerDivider: {
    marginBottom: 0,
  },
  searcField: {
    position: 'relative',
    display: 'flex',
    top: -35,
  },
})
