export enum Gender {
  male = 0,
  female = 1,
  other = 2,
}

export enum MaritalStatus {
  notMarried = 0,
  married = 1,
  divorced = 2,
  separate = 3,
  widower = 4,
  single = 5,
  other = 6,
}

export enum UsageType {
  balance = 0,
  overBalance = 1,
}
